var e = 24;
var somethingImportant = "We are coding here, so dont laugh";

function someFunc(t) {
    //input is the name of the parameter. This is a variable which will be created by JS at runtime. JS will assign
    // the value of the parameter with which this function is called, and assign that value to the variable called "input" here.
    // and it is important to notice that this variable "input" will only be accessible inside the block of this function.
    input = 7;
    return t;
}
//please notice that the variables yoiu define inside a function cannot be accessed outside of the function
var input = 59
    //console.log(someFunc(input));


//------x-----------x---------x